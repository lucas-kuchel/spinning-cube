{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "configure",
            "type": "shell",
            "command": "cmake -B build --preset debug",
        },
        {
            "label": "build",
            "type": "shell",
            "command": "cmake --build build --preset debug",
            "dependsOn": "configure",
        }
    ]
}